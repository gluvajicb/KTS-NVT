import { SectorPriceFormValidatorDirective } from './sector-price-form-validator.directive';

describe('SectorPriceFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SectorPriceFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
