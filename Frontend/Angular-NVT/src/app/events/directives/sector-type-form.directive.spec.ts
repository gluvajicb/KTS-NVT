import { SectorTypeFormDirective } from './sector-type-form.directive';

describe('SectorTypeFormDirective', () => {
  it('should create an instance', () => {
    const directive = new SectorTypeFormDirective();
    expect(directive).toBeTruthy();
  });
});
